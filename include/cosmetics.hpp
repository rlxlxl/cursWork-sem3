#pragma once

#include <string>
#include <fstream>
#include <chrono>
#include <thread>
#include <vector>
#include <iostream>
using namespace std;
void printBanner(const string& path);
void showLoading(int duration_ms = 4000);